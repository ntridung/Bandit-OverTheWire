# base64 encode/decode data and print to standard output
# Cú pháp
``` shell
base64 [OPTION] [file_name]
```
# OPTION
- none : mã hóa file đó dưới base64
- -d (decode) : giải mã file đã được mã hóa bằng base64
