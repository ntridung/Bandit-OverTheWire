# Con'*cat*'enate files and print on the standard output
# Cú pháp
``` shell
cat [OPTION] [FILE]
```

# OPTION

# Trường hợp đặc biệt
- Dashed Filename : use `cat ./ {FILE}` or `cat -- {FILE}`
- Space in Filename : use `cat "my file name.txt"` or `cat my\ file\ name.txt`
