# Concatenate files and print on the standard output

## cat {OPTION}... {FILE}...
### Dashed Filename 
use `cat ./ {FILE}` or `cat -- {FILE}`
### Space in Filename
use `cat "my file name.txt"` or `cat my\ file\ name.txt`
